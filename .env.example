# 服务端口（唯一来源）——修改后重启生效；Docker 用 `--env-file ../../.env` 读取
CHAT_API_PORT=4001      # chat 后端
CHAT_WEB_PORT=3002      # chat-web 前端
ADMIN_API_PORT=4002     # admin 后端
ADMIN_WEB_PORT=3003     # admin-web 前端
POSTGRES_PORT=5432      # postgres（仅宿主机映射，容器内仍 5432）
